<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.materialdoc">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialDoc">
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MaterialDoc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.RaisedButtonActivity"
            android:label="@string/title_activity_raised_button"
            android:theme="@style/Theme.MaterialDoc" />

        <activity
            android:name=".activity.FlatButtonActivity"
            android:label="@string/title_activity_raised_button"
            android:theme="@style/Theme.MaterialDoc" />

        <activity
            android:name=".activity.CircularProgressActivity"
            android:label="@string/title_activity_raised_button"
            android:theme="@style/Theme.MaterialDoc" />

        <activity
            android:name=".activity.LinearProgressActivity"
            android:label="@string/title_activity_raised_button"
            android:theme="@style/Theme.MaterialDoc" />

    </application>

</manifest>
